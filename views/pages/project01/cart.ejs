<%- include('./includes/head.ejs') %>
    <body>
        <%- include('./includes/navigation.ejs') %>
        <% if (products.length > 0) { %>
            <ul>
                <% products.forEach(p => { %>
                    <li><%= p.productData.productName %> ( <%= p.qty %> )</li>
                    <form action="./deleteCartItem" method="POST" name='deleteCartItem' id='deleteCartItem'>
                        <input type="hidden" name='prodId' id='prodId' value="<%=p.productData.id%>">
                        <input type="hidden" name='price' id='price' value="<%=p.productData.price%>">
                        <button class='button' type="submit">Delete</button>
                    </form>
                <% }) %>
            </ul>
        <% } else { %>
            <h1>No products in cart!</h1>
        <% } %>
    </body>
</html>