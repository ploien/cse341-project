<%- include('./includes/head.ejs') %>
    <body>
        <%- include('./includes/navigation.ejs') %>
        <main>
            <% if (orders.length <= 0) { %>
                <h1>No orders found.</h1>
            <% } else { %>
                <ul>
                    <% orders.forEach(order => { %>
                        <li>
                            <h1># <%= order._id %></h1>
                            <ul>
                                <% order.products.forEach( product => { %>
                                    <li><%= product.product.productName %> ( <%= product.quantity %> )</li>
                                <% }); %>
                            </ul>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
        </main>
        </body>
</html>