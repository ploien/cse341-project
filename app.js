//This is the server file (My first server). Was created by 
//following Academind tutorial.

const http = require('http'); //Importing Global HTTP module

const server = http.createServer();

server.listen(3000);


